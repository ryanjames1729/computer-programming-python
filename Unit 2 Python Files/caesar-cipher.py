# CDS - Programming
# Caesar Cipher Assignment
#
# Student Name:

testWord = "cat"
testShift = 19

originalWord = input("Give me a word to encrypt:>>")
shift = int(input("Type the key as an integer number:>>"))
newWord = ""                

# originalWord = testWord
# shift = testShift

# Create a loop that will run the length of the word with the algorithm inside the loop.


# Testcase output: "vtm"

# Print the output with the original word and the new word
